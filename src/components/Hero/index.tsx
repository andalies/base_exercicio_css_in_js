import { Container } from '../../GlobalStyles'
import FormWrapper, { HeroTitle } from './styles'

const Hero = () => (
  <FormWrapper>
    <Container>
      <HeroTitle>
        <h2>As melhores vagas para tecnologia, design e artes visuais</h2>
      </HeroTitle>
    </Container>
  </FormWrapper>
)

export default Hero
